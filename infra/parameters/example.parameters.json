{
  "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentParameters.json#",
  "contentVersion": "1.0.0.0",
  "_comment": [
    "SAFE EXAMPLE ONLY. This file is not desired state for any real environment.",
    "Use it to understand the supported parameter surface, then maintain actual values in an",
    "operator-owned location and pass that external parameter file explicitly to the bootstrap",
    "scripts. The illustrative values below select the eBay sandbox.",
    "",
    "Deliberately absent because they are release-specific and supplied by the scripts:",
    "  image, publicBaseUrl, accountDeletionEndpointUrl, deployApp, resourceGroupName.",
    "",
    "Deliberately absent because they are secrets held in Key Vault:",
    "  the connector API key, the eBay client id and client secret, and the eBay marketplace",
    "  account deletion verification token.",
    "",
    "Do not add account identifiers, operator contact information, or secrets to public examples."
  ],
  "parameters": {
    "environmentName": { "value": "example" },
    "location": { "value": "westus2" },
    "ebayEnvironment": { "value": "sandbox" },
    "ebayMarketplaceId": { "value": "EBAY_US" },
    "ebayDeliveryCountry": { "value": "US" },
    "ebayDeliveryPostalCode": { "value": "" },
    "logLevel": { "value": "info" },
    "minReplicas": { "value": 0 },
    "maxReplicas": { "value": 1 },
    "enableHealthAlerts": { "value": false },
    "alertEmails": { "value": [] },
    "alertSmsPhone": { "value": "" },
    "alertSmsCountryCode": { "value": "1" },
    "tags": { "value": {} }
  }
}
